import 'package:flutter/material.dart';

class ZoomDrawerHome extends StatelessWidget {
  const ZoomDrawerHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Home'),
    );
  }
}
