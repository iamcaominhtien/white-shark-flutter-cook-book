import 'package:flutter/material.dart';

class ZoomDrawerPayment extends StatelessWidget {
  const ZoomDrawerPayment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Payment'),
    );
  }
}
