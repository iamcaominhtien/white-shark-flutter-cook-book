import 'package:flutter/material.dart';

class ZoomDrawerNotification extends StatelessWidget {
  const ZoomDrawerNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notification'),
    );
  }
}
