import 'package:flutter/material.dart';

class ZoomDrawerPromos extends StatelessWidget {
  const ZoomDrawerPromos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Promos'),
    );
  }
}
