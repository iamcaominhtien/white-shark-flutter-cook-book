import 'package:flutter/material.dart';

class ZoomDrawerRateUs extends StatelessWidget {
  const ZoomDrawerRateUs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Rate us'),
    );
  }
}
