import 'package:flutter/material.dart';

class ZoomDrawerHelp extends StatelessWidget {
  const ZoomDrawerHelp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Help'),
    );
  }
}
